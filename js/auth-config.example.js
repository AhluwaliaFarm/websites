/**
 * Copy this file to auth-config.js and replace the hashes with your own.
 * Generate hashes: python3 scripts/hash-password.py "your-password"
 */
window.AUTH_CONFIG = {
  distributorPasswordHash: "REPLACE_WITH_SHA256_HASH",
  retailerPasswordHash: "REPLACE_WITH_SHA256_HASH"
};
